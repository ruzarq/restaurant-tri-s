// ГЛАВНЫЙ ФАЙЛ - ИНИЦИАЛИЗАЦИЯ ПРИЛОЖЕНИЯ

function init() {
    console.log('🍽️ Инициализация приложения "Ресторан Три С"...');
    
    // Загружаем данные
    loadData();
    
    // Инициализируем систему ролей
    initRoles();
    
    // Инициализируем все компоненты
    renderTables();
    renderMenuCategories();
    renderDishes();
    renderCart();
    renderKitchenOrders();
    renderStats();
    
    // Настраиваем автосохранение
    setupAutoSave();
    
    console.log('✅ Приложение успешно загружено!');
    showNotification('Система готова к работе!');
}

function setupAutoSave() {
    // Автосохранение каждые 30 секунд
    setInterval(() => {
        if (saveData()) {
            console.log('💾 Данные автоматически сохранены');
        }
    }, 30000);
    
    // Сохранение при закрытии страницы
    window.addEventListener('beforeunload', saveData);
}

// ЗАПУСК ПРИЛОЖЕНИЯ ПРИ ЗАГРУЗКЕ СТРАНИЦЫ
document.addEventListener('DOMContentLoaded', init);